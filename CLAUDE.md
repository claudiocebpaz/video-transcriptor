# Project Instructions

Before responding to any request, read and internalize [`AGENTS.md`](./AGENTS.md).

This project is **Docker-only**: all execution, linting, formatting, and type-checking commands must run via direct Docker image commands encapsulated by [`Makefile`](./Makefile) targets (`docker build` + `docker run`, no Docker Compose).
